from django.contrib.auth.forms import UserCreationforn
from django.contrib.auth import get, user godel

class UserRegistrationform(UsenCreatlonForm)
    class Meta:
        model = get user model()
        fields = ['cin','first_name','last_name','username','email','password1','password2']

def save(self, comitaTrue):
    user = super (UserRegistrationforn, seLf), save(connitafalse)
    if commit:
    user, save()
    return user